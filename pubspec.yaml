name: gg_is_flutter
description: Provides a function that detects if a directory contains a flutter project.
version: 1.0.8
publish_to: none
repository: https://github.com/inlavigo/gg_is_flutter.git

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  gg_project_root:
    git:
      url: https://github.com/ggsuite/gg_project_root.git
      ref: feat_test2
  path: ^1.8.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0